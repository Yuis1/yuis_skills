interface:
  display_name: "Architecture Enforcement"
  short_description: "Turn architecture constraints into executable checks, CI Gates, baseline ratchets, and exemptions"
  default_prompt: "Use $arch-guard to design automated enforcement for these architecture constraints."

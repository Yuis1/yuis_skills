interface:
  display_name: "Verification Evidence"
  short_description: "Judge whether behavioral, static, non-code, and legacy-test evidence truly proves a change"
  default_prompt: "Use $test-evidence to determine which credible verification evidence this change requires."

interface:
  display_name: "Multi-Agent Collaboration"
  short_description: "Control delegation, sessions, worktrees, single-Writer ownership, and Reviewer permissions"
  default_prompt: "Use $agent-team to organize this multi-Agent delegation and Reviewer isolation."
